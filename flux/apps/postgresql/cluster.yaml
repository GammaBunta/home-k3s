apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: postgresql
  namespace: postgresql
spec:
  instances: 1

  superuserSecret:
    name: postgresql-superuser

  storage:
    storageClass: nfs-nas
    size: 10Gi
